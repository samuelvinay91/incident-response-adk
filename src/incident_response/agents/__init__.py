"""Agent implementations for the Incident Response Orchestrator.

Each agent mirrors Google ADK's agent patterns (LlmAgent, SequentialAgent,
ParallelAgent, LoopAgent) but works without the actual google-adk package.
"""
